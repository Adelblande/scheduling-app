import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    display: "flex",
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: "#111",
    opacity: 0.8,
  },
  calendar: {
    backgroundColor: "#fff",
    borderRadius: 8,
    height: 380,
    width: 380,
  },
});
