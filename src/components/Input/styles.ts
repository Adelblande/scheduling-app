import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: 300,
    height: 40,
    borderWidth: 2,
    borderColor: "#6b299a",
    borderRadius: 4,
    paddingHorizontal: 16,
  },
});
